import 'package:flutter/cupertino.dart';

class ArchavedtaskScreen extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Center(
      child: Text(
        "Archaved Task",
            style:TextStyle(
          fontSize: 25.0,
        fontWeight: FontWeight.bold,

      ) ,
      ),
    );
  }
}
